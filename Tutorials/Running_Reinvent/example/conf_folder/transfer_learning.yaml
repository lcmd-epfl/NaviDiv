# Hydra configuration file for REINVENT transfer learning
# Override all ??? values on the command line or in a local config, e.g.:
#   python run_TL_reinvent.py \
#     --config-path /path/to/conf_folder \
#     output_dir=/my/output stage1_checkpoint=/my/prior.model ...

output_dir: ???
data_file: ???
n_head: 100000
n_tail: 20000
stage1_checkpoint: ???
train_filename: ???
validation_filename: ???
config_filename: ???
smiles_column: 4

parameters:
  num_epochs: 50
  save_every_n_epochs: 2
  batch_size: 100
  sample_batch_size: 2000
  standardize_smiles: false
  randomize_smiles: true
  randomize_all_smiles: false
  internal_diversity: true
  device: "cuda:0"
